import React from 'react';
import PropTypes from 'prop-types';
import { PageLayoutWithNav } from '../../components/layouts';

const Job = (props) => (
  <PageLayoutWithNav pageName="Job Info">Job</PageLayoutWithNav>
);

Job.propTypes = {
  //
};

export default Job;
